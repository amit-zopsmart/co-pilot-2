import { styled } from "@mui/material/styles";

export const Code = styled("code")(({ theme }) => ({
  display: "block",
  backgroundColor: "#02162f",
  padding: "8px 12px",
  position: "relative",
  borderRadius: "10px",
}));
